class Pay {
  int id;
  // String type;
  String description;
  var nodays;
  var nohours;
  var amount;

  Pay(
      {this.id,
      // this.type,
      this.description,
      this.amount,
      this.nodays,
      this.nohours});
}
